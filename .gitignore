# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Environments
.venv/
venv/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.hypothesis/
.coverage
.coverage.*
htmlcov/
coverage.xml
.mutmut-cache
.import_linter_cache/

# Secrets: never commit. Cassettes are recorded traffic and are scanned in CI,
# but raw credentials must never reach the working tree in the first place.
.env
.env.*
!.env.example
*.pem
*.key

# OS
.DS_Store

# Local editor and assistant configuration. Project tooling lives in Makefile,
# pyproject.toml and .github/; nothing under these paths is part of the project.
.devin/
.claude/
.cursor/
.windsurf/
.agents/
.idea/
.vscode/
