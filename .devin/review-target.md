# Review target

- Branch `plan/adr-0002-revision`, revision 22 (HEAD after the commit titled "revision 22").
- Scope as before. Your previous pass (3f7cda2, revision 21) returned APPROVE with three P3s; revision 22 applies them: I-JSON decoder named as an M2b deliverable in ledgergate.codec; identifiers spec scope covers the journal Request with arguments redacted per command field classes; note that approval_already_used is reachable only by a distinct artefact reusing an approval_id.
- Verify the three and confirm nothing else moved.
